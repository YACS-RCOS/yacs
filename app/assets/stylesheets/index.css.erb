/* CSS Resets for all main displaying elements. */
html, body, div, span, h1, h2, h3, h4, h5, h6, p, blockquote, pre, a, abbr, cite, code, em, img, q, s, strike, strong, sub, sup, ol, ul, li, fieldset, form, label, legend, table, caption, tbody, tfoot, thead, tr, th, td, article, aside, canvas, details, embed, figure, figcaption, footer, header, hgroup, menu, nav, output, section, summary, mark {
    margin: 0;
    padding: 0;
    border: 0px none;
    font-size: 100%;
    font: inherit;
    vertical-align: baseline;
}

/*
Note that we're using quite a few tags that are absent from any HTML standard
and thus have no inherent semantic meaning. Many of them are intended as
container elements, but the default behavior of browsers is to treat them as
inline, and thus their CSS styles will not display on them or their children
(in some cases). Use display:block on all elements that are supposed to be
containers, as well as anything else that ought to show in a block.
*/

/* Styles for all pages: background color, font, spacing. */
body {
    background-color:#efefef;
    color: #000;
    font-family: "Open Sans","Helvetica Neue",Helvetica,Arial,sans-serif;
}
div#content {
    padding: 70px 15px 0px 15px;
}
department-id, course-id, section-id {
    /* ID is a necessary part of the API but there is never any reason to show
       it to the user. */
    display:none;
}
    

/* Styles for the headerbar and its component table */
nav#headerbar {
    width:100%;
    height:60px;
    position:fixed;
    background-color:#933;
    color:#efefef;
    /* border-bottom: 2px #822 solid; */
}
table#navtable {
    width:100%;
    height:100%;
    border-collapse:collapse;
}
table#navtable td {
    text-align:center;
    vertical-align:middle;
}
table#navtable td.sidelink {
    width:160px;
    cursor:pointer;
}
#page-title {
    font-size:35px;
    font-weight:normal;
}
#page-title span.sub {
    font-size:16px;
}
#search {
    margin:auto;
}
#searchbar {
    /* font needs to be specified again for some reason, I think it's
       something to do with the input tag? */
    font-family: "Open Sans","Helvetica Neue",Helvetica,Arial,sans-serif;
    font-size: 16px;
    font-weight:100;
    width:32em;
    padding:3px 6px;
}
#schedule-btn {
    font-size:22px;
}

/* Single style for the loading image */
img#loading {
    position:absolute;
    top:50%;
    left:50%;
    /* browsers handle fractional pixel values differently, but the worst that
       can happen is that it's 1/2 pixel off center. So it's best to use the
       real values (half of the image width and height) here. */
    margin-top: -4.5px;
    margin-left: -62.5px;
}

/* Single style for any error messages that appear */
.error {
    
}

/* Styles for the home page. Note that <schools> and <departments> can expect
 to be removed from the DOM and should NOT be styled.*/
table#homeTable {
    display:block;
    margin-top:6px;
    margin-left:auto;
    margin-right:auto;
}
school {
    display:block;
    width:600px;
    padding:0px 10px;
}
school-name {
    display:block;
    font-size:21px;
    font-style:italic;
    border-bottom: 1px black solid;
}
departments {
    display:block;
    margin-bottom: 15px;
    font-size:17px;
}
department {
    display:block;
    cursor:pointer;
    padding:2px 5px;
    width:600px;
}
department:hover {
    color: #fff;
    background-color: #933;
}
department department-code {
    font-family:"Andale Mono","Courier New",monospace;
    margin-right:0.2em;
    font-size:120%;
}
noscript {
    color:#933;
    font-weight:bold;
    font-size:150%;
}


/* Styles for the course list pages. */
course {
    display:block;
    margin: 0px 20px;
    padding:15px;
    /*border-radius: 15px;*/
    border-bottom: 1px #bbb solid;
    cursor:pointer;
}
/* Note for anyone maintaining this in the distant future:
   The original design goal for the course list is for the course and all its
   sections to get highlighted when hovering over it, and for the sections
   and NOT their parent course to get highlighted when hovering over them. In
   the design there was only one hover color. I cannot make that work with pure
   CSS, and doing it in Javascript is incredibly ugly (code), so I'm using two
   different highlight colors. If CSS4 is out when you're looking at this code,
   there should be some way to do it with negation selectors.
   */
course:hover {
    background-color:#e7e7e7;
}
course-name {
    font-size: 130%;
}
course-number, course department-code, course-credits {
    font-size:110%;
}
course department-code {
    margin-left:1em;
}
course-credits {
    margin-left:0.7em;
}
course-description {
    display:block;
    font-size:87%;
}
section {
    display:block;
    margin:0px 0px 0px 5px;
    padding:4px 4px 4px 8px;
    /* border-radius: 8px; */
}
section:hover {
    background-color: #e0e0e0;
}
section.selected {
    color: #efefef;
    background-color: /*#ce5656;*/#933;
}
section.closed {
    color: #888;
}
section-crn, section-seats, section-seats-taken {
    display:none; /* hidden from the user in the course view */
}
periods {
    font-size:80%;
    display:inline-block;
    margin-left:0em;
    float:right;
    padding:0.15em 0;
}
periods.noperiods {
    font-weight: bold;
}
period {
    margin-left:1em;
}
period-day {
    font-weight:bold;
}

/* NOTE: Should have it so that hovering over a course makes all its subsections appear hovered over. This may not be possible without CSS3. */

/* Styles for the schedule page */
#schedulebar {
    text-align: center;
    margin-left: auto;
    margin-right: auto;
    font-size: 130%;
    /* note to maintainers: if the general user-select is standard by now,
       please remove the vendor ones and replace with "user-select:none;" */
    -moz-user-select: none;
    -webkit-user-select: none;
    -ms-user-select:none;
}
.scheduleswitch {
    padding:0px 20px;
    cursor:pointer;
}
.scheduleswitch.disabled {
    color: #999;
    cursor: default;
}

div#scheduleContent {
    margin-bottom:30px;
}

div#scheduleTable {
    display:table;
    margin: 10px auto auto auto;
    padding-right: 30px;
    /* padding-right must be == to the width of .narrowcol to center the
       table correctly */
    font-size:16px;
}
div#scheduleTable ul {
    display:block;
    float:left;
    width:200px;
    border-right: 1px #bbb solid;
    text-align:center;
    list-style-type:none;
}
div#scheduleTable ul.narrowcol {
    width:60px;
}
div#scheduleTable ul.narrowcol li {
    text-align:right;
    padding-right:0.5em;
}
div#scheduleTable ul li {
    height:24px;
    border-bottom:1px #bbb solid;
}
div#scheduleTable ul li.hour {
    height:2.8em;
    padding-bottom:1px; /* account for the missing border space */
}

/* Deselection list styles */
div#deselectContent {
    display:block;
    width:60%;
    margin: 20px auto 0 auto;
}
div#deselectContent department-code, div#deselectContent course-number,
div#deselectContent course-credits, div#deselectContent course-description {
    display:none;
}

/* Course colors used by YACS Original: (border/text/background)
   #B62/#720/#FDC
   #B44/#722/#EDD
   #994/#661/#EED
   #448/#227/#DDE
   #499/#166/#DEE
   #949/#616/#EDE
   */

/* Styles for courses on the schedule */
/* "div#scheduleTable ul li" needed for CSS precedence rules */
div#scheduleTable ul li.course { 
    border-bottom:none;
    padding: 0px 4px 0px 4px;
    overflow: hidden;
    text-overflow:ellipsis;
}
div#scheduleTable ul li.course.end30 {
    border-bottom:1px #bbb solid;
}
.c1 {
    color: #720;
    background-color: #fdc;
    border-left: 2px #b62 solid;
}
.c2 {
    color: #722;
    background-color: #edd;
    border-left: 2px #b44 solid;
}
.c3 {
    color: #661;
    background-color: #EED;
    border-left: 2px #994 solid;
}
.c4 {
    color: #227;
    background-color: #DDE;
    border-left: 2px #448 solid;
}
.c5 {
    color: #166;
    background-color: #DEE;
    border-left: 2px #499 solid;
}
.c6 {
    color: #616;
    background-color: #EDE;
    border-left: 2px #949 solid;
}
div#scheduleTable .closed {
    background-image: url("<%=asset_path('generic-closed.png')%>");
    background-repeat: repeat;
    color: #000;
}