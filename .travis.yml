sudo: required

language: ruby

services:
  - docker
  - postgresql

env:
  - RAILS_ENV=test

before_install:
  - docker-compose build

script:
  - docker-compose run web rake db:create db:migrate
  - docker-compose run web rake
