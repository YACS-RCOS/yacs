sudo: required

services:
  - docker

env:
  - RAILS_ENV=test

install:
  - script/yacs-build
  - script/yacs-startd && docker ps
  - sleep 10
  - script/yacs-dbsetup

script:
  - docker-compose run web bundle exec rake db:create db:migrate
  - script/yacs-test