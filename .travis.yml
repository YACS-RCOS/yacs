sudo: required

services:
  - docker

env:
  - RAILS_ENV=test

install:
  - bin/yacs-build
  - bin/yacs-startd && docker ps
  - sleep 10
  - bin/yacs-dbsetup

script:
  - docker-compose run web bundle exec rake db:create db:migrate
  - bin/yacs-test