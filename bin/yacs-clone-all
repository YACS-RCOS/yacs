#!/bin/bash
cd $(cd -P -- "$(dirname -- "$0")" && pwd -P)/../develop

REPOS="yacs
yacs-web
yacs-admin
yacs-users
yacs-nginx
yacs-malg"

for repo in $REPOS; do
  uri="git@github.com:YACS-RCOS/$repo.git"
  git clone $uri
done

cd yacs && git checkout micro
